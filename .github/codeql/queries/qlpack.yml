# scope/name where is scope is a user or organization that you have write access to.
name: cmr-projects/codeql-queries-custom
version: 0.0.1
dependencies:
  # This uses the latest version of the codeql/java-all library.
  # You may want to change to a more precise semver string.
  codeql/csharp-all: "*"
